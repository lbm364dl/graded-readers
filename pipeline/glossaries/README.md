Each file contains extra characters (proper nouns, place names, story-specific terms)
that are allowed regardless of level for a specific reader.

One character per line, or all on one line. Lines starting with # are comments.
